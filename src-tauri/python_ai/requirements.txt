# Core dependencies - GPU support
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# ONNX export dependencies (untuk conversion .pt → .onnx)
onnx>=1.12.0,<1.20.0
onnxruntime-gpu>=1.15.0

# Image processing (minimal - only for legacy --infer mode)
numpy>=1.23.0
# opencv-python>=4.8.0  # Removed - annotated images now in Rust
pillow>=9.0.0  # Only for legacy --infer mode image validation
pyyaml>=6.0

# Geospatial libraries REMOVED - now handled in Rust:
# geojson>=3.0.0  # Moved to Rust
# geopandas>=0.14.0  # Moved to Rust (requires many dependencies)
# fastkml>=0.12  # Moved to Rust
# shapely>=2.0.0  # Moved to Rust
